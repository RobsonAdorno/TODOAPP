import 'package:flutter/material.dart';
import 'core.dart';

titleText(String text) => Text(
      text,
      style: TextStyle(
          color: colorToTheText, fontSize: 21, fontWeight: FontWeight.bold),
    );
